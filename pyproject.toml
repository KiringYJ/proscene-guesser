[project]
name = "proscene-guesser-media"
version = "0.1.0"
description = "Maintainer-only media acquisition tools for ProScene Guesser"
requires-python = ">=3.13"
dependencies = [
  "yt-dlp[curl-cffi,default]>=2026.7.4",
]

[tool.uv]
link-mode = "copy"
package = false
